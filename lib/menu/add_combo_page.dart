import 'package:flutter/material.dart';

class AddComboPage extends StatelessWidget {
  const AddComboPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Add Combo"),
      ),
      body: Text("Add Combo"),
    );
    ;
  }
}
